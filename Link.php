<?php
foreach (range(a, g) as $letter) { //Диапазон символов для цыкла.
	echo $letter;

	 if($i == (count($letter) -1)){ // Условие для разделения.
      echo ", ";
    }
}

 foreach (range (a,a) as $letter) { //Диапазон символов для цыкла.
	echo $letter;
}

 foreach (range (a,a) as $letter) { //Диапазон символов для цыкла.
	echo $letter;
	
	 if($i == (count($letter) -1)){ // Условие для разделения.
      echo ", ";
    }
}

 foreach (range (a, b) as $letter) { //Диапазон символов для цыкла.
	echo $letter;
}
echo ". ";
      echo "<br>";
      echo "<br>";


?> 

